[pytest]
markers =
    slow: marks tests as slow (opt-in with --runslow)
    integration: integration tests using real spawned processes
asyncio_mode = auto
addopts = -ra
# Slow tests are no longer skipped by default. To run only the fast
# tests, use ``pytest -m "not slow"`` explicitly.
